import { GetScopeNameByIdPipe } from './get-scope-name-by-id.pipe';

describe('GetScopeNameByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetScopeNameByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
